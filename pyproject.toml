[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "holoptycho"
version = "0.1.0"
requires-python = ">=3.11,<3.12"

[tool.setuptools]
packages = ["holoptycho", "holoptycho.edgePtychoViT"]
